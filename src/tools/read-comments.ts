import { z } from "zod"; import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"; import { asMcpResult } from "./result.js"; import { unavailable } from "../core/contracts.js";
const graphId = z.string().regex(/^\d+$/, "ID должен быть числовым Instagram Graph ID.");
export function registerCommentTools(server:McpServer) { const reply=async()=>asMcpResult(unavailable("comments","Комментарии отключены в текущей read-only поставке.","Комментарии будут доступны только после отдельной реализации и диагностики разрешений.")); server.tool("ig_get_comments","Получить комментарии медиа.",{media_id:graphId,limit:z.number().int().min(1).max(100).optional()},reply); server.tool("ig_get_comment","Получить комментарий.",{comment_id:graphId},reply); server.tool("ig_get_replies","Получить ответы на комментарий.",{comment_id:graphId,limit:z.number().int().min(1).max(100).optional()},reply); }
